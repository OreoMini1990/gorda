# 브랜드 에셋

## 로고

- **경로**: `gorda-logo.png` (이 폴더에 저장)
- **사용 위치**: 헤더 좌측 로고 — `components/landing/Logo.tsx`에서 `/brand/gorda-logo.png`로 참조
- 첨부하신 GORDA COMPANY 로고 이미지를 이 폴더에 `gorda-logo.png` 이름으로 저장해 두시면 헤더에 표시됩니다.
- 파일이 없으면 SVG 폴백 로고가 자동으로 표시됩니다.
